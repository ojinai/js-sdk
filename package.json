{
  "name": "ojin-client",
  "version": "0.1.0",
  "description": "Ojin Speech-To-Video WebSocket client SDK for TypeScript",
  "type": "module",
  "main": "dist/cjs/index.cjs",
  "module": "dist/esm/index.js",
  "types": "dist/types/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.cjs",
      "default": "./dist/esm/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "test:wire-compat": "vitest run tests/fixtures/wire.test.ts",
    "lint": "biome check --fix src/ tests/",
    "lint:check": "biome check src/ tests/",
    "format": "biome format --write src/ tests/",
    "format:check": "biome format src/ tests/",
    "precommit": "pnpm lint:check && pnpm typecheck && pnpm test",
    "prepush": "pnpm precommit && pnpm security",
    "security": "pnpm security:secrets && pnpm security:sast && pnpm security:audit",
    "security:secrets": "gitleaks detect --redact --no-banner",
    "security:sast": "semgrep scan --config=auto --error --quiet",
    "security:audit": "pnpm audit --audit-level=moderate",
    "clean": "rm -rf dist coverage",
    "prepublishOnly": "pnpm run build"
  },
  "keywords": [
    "ojin",
    "speech-to-video",
    "websocket",
    "avatar",
    "stv"
  ],
  "author": "Journee",
  "license": "Apache-2.0",
  "homepage": "https://github.com/journee-live/ojin",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@rollup/plugin-commonjs": "^28.0.0",
    "@rollup/plugin-json": "^6.1.0",
    "@rollup/plugin-node-resolve": "^16.0.0",
    "@rollup/plugin-typescript": "^12.1.0",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "rollup": "^4.0.0",
    "tslib": "^2.8.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "peerDependencies": {
    "@types/ws": "^8.5.0"
  },
  "peerDependenciesMeta": {
    "@types/ws": {
      "optional": true
    }
  }
}
